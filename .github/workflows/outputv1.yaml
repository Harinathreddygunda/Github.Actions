name: outputv1-demo
on: workflow_dispatch
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step one
        id: step1
        run: echo "version = 0.01" >> $GITHUB_OUTPUT
      - name: step two
        run: echo "This version is ${{ steps.step1.outputs.version }}"
